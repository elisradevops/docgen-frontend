// Trace table work-item-type colors — must match tablePresentation.ts in docgen-content-control
// so the dialog preview is consistent with what appears in the generated document.

export const COLOR_REQ = '#DBE5F1';       // Requirement / System / PCR rows
export const COLOR_TEST_CASE = '#E4DFEC'; // Test Case / Software rows
export const COLOR_UNCOVERED = '#FFC7CE'; // Uncovered / trace gap rows
